#This file is used to package the ConsolidatedShipmentLines plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.13.3
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-consolidated-shipment-lines"
description = "Provide consolidated line items for Sales Order Shipment reports"
dynamic = ["version"]
authors = [
    { name = "Oliver Walters",email = "oliver.henry.walters@gmail.com" },
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://github.com/SchrodingersGat/inventree-consolidated-shipping-report"

[project.entry-points."inventree_plugins"]
ConsolidatedShipmentLines = "consolidated_shipment_lines.core:ConsolidatedShipmentLines"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "consolidated_shipment_lines.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["consolidated_shipment_lines"]
